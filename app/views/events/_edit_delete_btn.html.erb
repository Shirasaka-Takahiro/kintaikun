<% if user_signed_in? %>
<span><%= link_to '編集', edit_event_path(event.id), class: 'btn' %></span>
<span><%= link_to '削除', event_path(event.id), method: :delete, data:{confirm: "削除してもよろしいですか？"}, class: 'btn' %></span>
<% end %>